import theme from './materialUi';

export default {
  navRight: { fontFamily: theme.typography.fontFamily },
  navButton: {
    backgroundColor: theme.palette.primary.main,
    fontFamily: theme.typography.fontFamily,
  },
  signInButton: {
    backgroundColor: theme.palette.primary.main,
    fontFamily: theme.typography.fontFamily,
  },
  button: {
    backgroundColor: theme.palette.primary.main,
    fontFamily: theme.typography.fontFamily,
  },
  a: {
    color: theme.palette.primary.main,
  },
  oAuthSignInButton: {
    color: 'white',
    backgroundColor: theme.palette.primary.main,
    fontFamily: theme.typography.fontFamily,
  },
  strike: {
    display: 'none',
  },
  googleSignInButton: {
    display: 'none',
  },
};
